"""L3 Domain Capabilities — 把平台原料组装成可被特性消费的成品。

此层允许 import L1 (core) 和 L2 (services/platform)；
禁止 import L4 (features/)。
P2 阶段会把 ../../{agents,capabilities,tools,runtime,knowledge} 全部迁入此目录。
详见 AGENTS.md §2。
"""
